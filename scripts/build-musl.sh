#!/bin/bash
set -euo pipefail
# Build musl libc for wasm32posix target
# Custom arch files live in musl-overlay/ and are symlinked into musl/ at build time
echo "Error: build-musl.sh is not yet implemented (see Task 5)" >&2
exit 1
